# @boq/web

Next.js frontend for BOQ Pro. Calculations run client-side via `@boq/engine`.

```bash
# from repo root
npm install
npm run dev
```

Vercel: set Root Directory to `apps/web` and include files outside the root directory.
